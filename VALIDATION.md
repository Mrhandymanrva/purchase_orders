# Validation record — September 14, 2026

- Engine 1.0.3 adds explicit, unlimited merchant exemptions. Six new automated tests cover uncapped versus capped/zero/pending rules, exact vendor identity, duplicate/refund/zero/missing-vendor safeguards, unchanged financial records and scorecard totals, CSV inclusion, audited pending-rule updates, separate approval, API persistence and invalid limits. The prior 115 tests passed in the full run; all six new tests passed after correcting the API test's local Origin fixture.
- Browser validation submitted an unlimited Wawa rule using fictional data and verified that it displays “No amount limit” and remains “Awaiting approval.” Existing capped rules retain their limits.
- A read-only production QuickBooks check identified 21 current purchases across 10 fuel merchants (7-Eleven, Wawa, Shell, BP, Sunoco, Sheetz, Raceway, Murphy Express, Marathon and Kroger Fuel). Every one used the Van Gasoline expense category. The user approved merchant-wide exemptions, including convenience-store purchases, without deleting source purchases or POs.

- Railway production pilot deployed successfully from GitHub to the existing Purchase Orders project. The Docker image built, the pre-deploy migration completed against Railway PostgreSQL, `/api/health` returned 200 with live mode, anonymous `/api/state` returned 401, and authenticated page/state reads returned 200. The workspace was empty (revision 0, no source records or audit events); no sample financial data was seeded.
- The private Postgres reference, HTTPS origin, operator authentication and 32-byte token encryption key are configured in Railway Variables. Secret values were neither printed nor committed. At the initial deployment, ST/QBO credentials and source account scope were not yet configured, so no financial-source sync was attempted.
- New Railway services rejected the legacy config-file path. Migration and health-check settings were applied directly through the service API; `scripts/configure-railway.graphql` reproduces that configuration. The obsolete `railway.json` was removed.

- 115 automated tests passed on Node.js / Windows, including permanent card ownership across historical/future purchases and refunds, duplicate-card rejection, ownership precedence, closed-card/inactive-employee retention, resync persistence, and child-only imports without parent double counting. Spreadsheet tests cover date-free two-column files, 25-card idempotence, corrections across full history, ignored legacy dates, omissions, invalid rows, formula rejection, signed preview tampering/expiry/policy changes, stale revisions and current-mapping export round trips. The final template regression was rerun through the app's namespace-compatible reader after its initial direct ExcelJS read exposed that library's known prefixed-XML limitation.
- Engine 1.0.2 coverage tests cover inclusive card-history boundaries, out-of-range POs retained without unpaid exceptions, matching an older PO to a later imported charge, expanded/unknown coverage, invalid-bound rejection, review/variance exclusion, retained CSV and Van Stock/PO totals, manual-decision precedence, reproducible sync audit context, and report API consistency.
- A read-only completeness audit at 2026-09-14 01:39 UTC read 13,758 tenant POs across all 69 pages, with zero duplicate IDs and agreement with the API total. Independent queries for all six documented statuses found no extra POs. Within the selected Richmond unit and June 3–September 13 window, all 757 non-canceled POs matched the saved IDs, dates, source statuses and totals ($129,284.77); missing, extra and changed counts were zero. The 22 canceled Richmond POs were intentionally excluded. No financial source records were changed.
- The unmatched-count diagnosis found 667 unlinked POs dated before the September 1 start of imported card history, plus vendor-name differences between QuickBooks Home Depot/Lowe's and ST 1 Home Depot/2 Lowes. Those two exact aliases were staged as inactive suggestions, then explicitly approved by the user and enabled through audited actions. Their local preview added candidate links for 54 POs while retaining ambiguity/duplicate checks; this did not force matches. Eighty vendorless QuickBooks purchases remain review exceptions.
- Bulk assignment tests cover 25 cards without dates or typed reasons, duplicate cards in either row order even with disjoint legacy periods, missing people/accounts, duplicate edits, rejection without mutation, single-row preservation of unrelated assignments and historical notes, authenticated/origin-checked API saves, PostgreSQL persistence, audit integrity and stale-revision rollback. Browser checks saved two fictional assignments together with no date fields; the grid has three columns and the save toolbar and pinned row actions remain visible in a narrow viewport. Previous checks covered retaining other drafts after Save row and switching app sections. No live card assignment was changed by these browser checks.
- The first full live sync identified a QuickBooks purchase without a vendor name. Engine 1.0.1 imports these as Missing vendor review items with unchanged signed amounts and references. Regression tests prevent 1:1, grouped, refund and alias-based automatic matches or exemptions for missing vendor identity; reports retain the purchases, suggestions exclude them, and explicit manual reviews remain fingerprint-bound.
- ServiceTitan regression checks cover active-only people requests, defensive inactive filtering (including blank-name inactive records), active-name/identity validation, preserved historical assignments, independent named business-unit discovery, tenant/environment binding, selected-unit PO filtering, authenticated/origin-checked scope saves, PostgreSQL persistence, audited scope changes and stale-revision rejection. Live read-only diagnostics confirmed the Richmond and Hampton business units and identified an inactive unnamed employee that blocked the earlier directory refresh.
- Live directory refresh loaded 22 active technicians and 106 active employees, with zero inactive people or blank names. Richmond was selected through the audited scope action; setup readiness passed without a manual ST business-unit variable. Existing mappings and the financial snapshot were unchanged. The PO adapter tolerates null business-unit IDs while excluding unscoped POs from branch-restricted imports; its regression test also covers missing IDs.
- A successful live sync imported 327 card purchases, including 80 with missing vendor names. A read-only source check then found 757 Pending and 22 Canceled Richmond POs dated June 3–September 13, 2026. The former Pending exclusion explained the zero imported POs. Regression checks now include Pending POs within the selected branch, preserve their source status and amount, and continue excluding canceled and out-of-scope POs. Source details display the original ServiceTitan status without changing the source workflow.
- QuickBooks onboarding tests exercise browser-bound single-use state, expiry, environment/client binding, successful callbacks and replay rejection, secure cookies, canceled callbacks, encrypted token capture/rotation, company-switch rejection, atomic token/metadata/audit rollback, mapping uploads before connection, independent card discovery, validated child selection, saved-scope import filtering and parent-assignment rejection. Financial-provider responses are mocked; PostgreSQL behavior runs through PGlite. Real Intuit consent still requires the owner to approve the company interactively.
- Card-selection form regression checked in a local browser using fictional accounts: Save stays available, a blank or whitespace-padded short reason focuses the field and shows an inline error, an empty card selection shows its own error, and a valid selection submits and displays the server response near Save. The required reason and five-character minimum are visible before submission. The temporary preview was removed before the production build; no live card selection was changed by these checks. All 88 automated tests passed again.
- Connection setup tests cover aggregate missing-setting messages, secret-free authenticated diagnostics, stored OAuth token presence, invalid environments/dates/keys, stopping both sync paths before any provider request, and parent-only dropdown discovery without widening purchase imports. The setup endpoint requires authentication and returns no-store responses. Settings presence does not certify provider authentication or API permissions.
- Public legal-page middleware tests allow exactly `/eula` and `/privacy-policy` (plus the existing health route) without credentials while keeping workspace, API, nested and lookalike paths protected. Both legal pages were visually reviewed in the browser, including cross-page navigation and the confirmed operator name.
- VAN STOCK / VS classification checks passed for exact type aliases, leading number/summary labels, case/whitespace normalization, rejected substring/prose matches, unavailable types, signed refunds and source dates. Adapter-to-scorecard tests preserve ST source labels; CSV carries the classification rule version. Browser source details show a VAN STOCK badge and its ST label/field, with automatic type aliases visibly selected.
- Scorecard tests cover leap years and ISO week boundaries, amount conservation, PO-date filtering, cross-period attribution, shared POs counted once, refunds, uncharged POs, explicit Van Stock type settings, stable duplicate-name identities, and read-only source directories stripped of unnecessary employee metadata.
- Browser checks: saved three rows through ST person dropdowns; another row's unsaved draft survived the first save. Taylor Reed's sample Van Stock changed from $914.99 in September/Q3 to $625.00 in week 37, and $0 in 2025. Source details exposed the underlying POs and attribution basis. Responsive scorecard layout stayed within a 773-pixel viewport, with horizontal scrolling confined to tables.
- Scorecard HTTP checks passed for month/week/year exports, technician filtering, data coverage, invalid reporting periods, and exported ST mapping identities (`node scripts/smoke-scorecards.cjs`).
- Next.js 15.5.25 production compilation, TypeScript validation and standalone build passed.
- PostgreSQL integration tests ran through PGlite: schema creation, transactional persistence/reload, rollback, and audit UPDATE/DELETE/TRUNCATE protection passed.
- Local HTTP checks passed: page, state, health, manual ownership assignment, audit, resync, individual CSV export, invalid input rejection, cross-origin rejection and stale-revision conflict.
- Subaccount HTTP checks passed: permanent mapping save without dates, audit event, duplicate-card batch rejection, repeated-save identity preservation, unchanged resync and individual CSV with subaccount IDs and linked POs. These checks used sample accounts only.
- Browser checks passed: individual filter changed card totals and result rows, report URL followed the person filter, detail panel showed the correct card user and PO, and a saved manual review displayed Confirmed.
- Browser spreadsheet checks previously passed with 25 fictional cards: initial import saved; repeat preview showed 25 unchanged; one employee correction showed 1 updated / 24 unchanged; applying the correction updated the individual filter and linked PO-2041. The revised date-free template's two sheets were rendered and visually reviewed; automated checks verify its six headers, blank-row rejection, and a filled two-column row preserving a leading-zero account ID.
- Production standalone startup passed and denied access with missing authentication/database configuration, including when DEMO_MODE was accidentally enabled.
- npm dependency audit reported zero vulnerabilities after overriding PostCSS to 8.5.28 and ExcelJS's UUID dependency to 11.1.1.

Not performed: closed-period reconciliation against actual statements, backup/restore validation, or a separate certificate-verified PostgreSQL TLS test. The deployed app connects through Railway's private network. No external business records were modified. The local sample server uses fictional data and its memory resets on restart; the deployed workspace uses PostgreSQL. Complete README go-live validation using authorized credentials and your actual card-user mapping.
